
Build "cpq" program using makefile. Run:
    make
in this directory.

Code is in "src/" and "lib/" directories.
Test inputs and outputs in directory "cpl-examples/".
"quad.py" is a python interpreter for Quad output language.

