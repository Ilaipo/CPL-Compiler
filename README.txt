
Build "cpq" program using makefile. Run:
    make
in this directory.

Code is in "src/" and "lib/" directories. "lib/" contains C implementations of helper functions, data structures, etc.
Test inputs and outputs in directory "cpl-examples/": Input code (CPL language) is in '.ou' files; Output intermediate representation (Quad language) is in '.qud' files.

